obj-m += virtio_gpu/

obj-m += virtio_snd/

obj-$(CONFIG_VIRT_WIFI) += wlan_simulation/

obj-${BUILD_GOLDFISH_DRIVERS} += goldfish_drivers/

obj-y += virtio_video/

obj-y += arm_scmi/

obj-y += scmi_iio/

obj-y += virtio_mmio/
